export type Material = {
  id: number;
  title: string;
  slug: string;
};

export type TargetCustomers = {
  id: number;
  title: string;
  slug: string;
};

export interface ImageType {
  id: number;
  url: string;
}
